<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="4.5" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
   	<!--
	    Specifies all targets that should be done after a successful build
      This target is still empty and does not do anything
    -->
   <Target Name="AfterBuildTargets" AfterTargets="Build">
  
   </Target>  
</Project>

