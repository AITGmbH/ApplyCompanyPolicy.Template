<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="native" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <!--
	    Specifies all targets that should be done after a successful build for the native framework
      This target is still empty and does not do anything
  -->
  <Target Name="AfterBuildTargets" AfterTargets="Build">

  </Target>
</Project>

